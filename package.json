{
  "name": "parse-bigint",
  "description": "Parses a number-like value into a BigInt with support for scientific notation, underscores, commas, and base prefixes.",
  "version": "0.0.1",
  "license": "AGPL-3.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest --reporter=verbose",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "author": "ryan goree (https://github.com/ryangoree)",
  "repository": {
    "type": "git",
    "url": "https://github.com/ryangoree/parse-bigint"
  },
  "publishConfig": {
    "access": "public"
  }
}
