{
  "name": "parse-bigint",
  "version": "1.0.1",
  "description": "Parses a number-like value into a BigInt with support for scientific notation, underscores, commas, and base prefixes.",
  "license": "AGPL-3.0",
  "author": "ryan goree (https://github.com/ryangoree)",
  "repository": "https://github.com/ryangoree/parse-bigint",
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "@types/node": "^24.0.10",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.build.json",
    "watch": "npm run build --watch",
    "test": "node --test src/**",
    "test:watch": "node --test --watch src/**",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "typesVersions": {
    "*": {
      ".": [
        "./dist/index.d.ts"
      ]
    }
  },
  "publishConfig": {
    "access": "public"
  }
}
